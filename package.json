{
  "name": "unifiedtrendviewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "vite",
    "dev:api": "dotnet run --project server",
    "build": "npm run build:web && npm run build:api",
    "build:web": "tsc && vite build",
    "build:api": "dotnet build server",
    "publish:api": "dotnet publish server -c Release -o publish/server",
    "publish:windows": "npm run build:web && dotnet publish server -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o publish/win-x64",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "dayjs": "^1.11.21",
    "echarts": "^6.1.0",
    "papaparse": "^5.5.4"
  }
}
